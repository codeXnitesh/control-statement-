n = int(input("Enter a number: "))
for i in range(n):
   left_stars = "*" * ( 2 -  n - i )
   spaces = " " * (  i + 1)
   right_stars = "*" * (n - i)
   print(left_stars + spaces + right_stars)
